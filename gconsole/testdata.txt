initP(-0.1, -0.1, -0.1, -0.1, -0.2);

drawTraverse(
    new Angle(288),               /* Начальный дирекционный угол */
    [101.2,110.0,196.4,188.9],    /* Длины отрезков */
    [168, 130, 98, 306.5, 218.5], /* Углы поворота */
    1/20                          /* Увеличение */
);

/* Вычисления на станции тахеометрического хода */
traverseStation(
    1.30,                   /* Высота инструмента */
    new Angle(0, 1.5),      /* Горизонтальный отсчёт на предыдущий пункт (КЛ) */
    new Angle(168, 14.5),   /* Горизонтальный отсчёт на следующий пункт (КЛ) */
    new Angle(181, 31),     /* Горизонтальный отсчёт на предыдущий пункт (КП) */
    new Angle(349, 44.5),   /* Горизонтальный отсчёт на следующий пункт (КП) */
    0,                      /* Высота метки на рейке на предыдущем пункте */
    1,                      /* Высота метки на рейке на следующем пункте */
    null,                   /* Вертикальнйы отсчёт на предыдущий пункт (КЛ) */
    null,                   /* Вертикальнйы отсчёт на следующий пункт (КЛ) */
    new Angle(1, 23),       /* Вертикальнйы отсчёт на предыдущий пункт (КП) */
    new Angle(-1, -24),     /* Вертикальнйы отсчёт на следующий пункт (КП) */
    0,                      /* Отсчёт по дальномеру на предыдущий пункт */
    0,                      /* Отсчёт по дальномеру на предыдущий пункт */
    101.5,                  /* Отсчёт по дальномеру на следующий пункт */
    101.5,                  /* Отсчёт по дальномеру на следующий пункт */
    -2.74                   /* Обратное превышение, со следующего пункта */
);
